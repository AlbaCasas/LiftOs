import { PageShell } from "@/components/page-shell";

export default function BlocksPage() {
  return (
    <PageShell
      title="Blocks"
      description="Block authoring comes after the Athletes list."
    />
  );
}
